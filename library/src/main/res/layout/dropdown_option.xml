<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/preference_padding"
    android:background="?attr/selectableItemBackground"
    android:clickable="true">

    <View
        android:id="@+id/preference_dropdown_anchor"
        android:layout_width="0dp"
        android:layout_height="0dp"/>

    <TextView
        android:id="@+id/preference_dropdown_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/PreferenceTitleText"
        android:maxLines="1"
        tools:text="Preference title"/>

    <TextView
        android:id="@+id/preference_dropdown_selection"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        style="@style/PreferenceDetailText"
        tools:text="Preference description"/>

</LinearLayout>